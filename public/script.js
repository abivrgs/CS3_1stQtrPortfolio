let age = 15;
let maxAge = 100;
let numPerDay = 4;

let totalRequired = (numPerDay * 365)*(maxAge - age);

document.write('You will need ' + totalRequired + ' cream o packs to last you until the ripe old age of ' + maxAge + '.');